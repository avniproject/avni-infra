---

- name: reload nginx launchpad_forms
  service:
    name: nginx
    state: reloaded

- name: restart launchpad_forms service
  systemd:
    name: "{{ launchpad_forms_instance_name }}-svc"
    state: restarted
    daemon_reload: yes
